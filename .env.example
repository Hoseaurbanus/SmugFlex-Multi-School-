# API Configuration
VITE_API_BASE_URL=https://your-domain.com/api

# Backend Configuration
CPANEL_HOST=https://your-cpanel-domain.com

# Application Settings
VITE_APP_TITLE=SMugFlex 2.0
VITE_APP_VERSION=2.0.0

# Production Settings (set to false only for local development)
VITE_DEV_MODE=false

# JWT Secrets (generate with: php -r "echo bin2hex(random_bytes(32));")
SUPER_ADMIN_JWT_SECRET=CHANGE_ME_TO_RANDOM_64_HEX_CHARS
